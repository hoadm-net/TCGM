# Adapter artifacts

Parquet files and `validation.json` are written here by adapter scripts.

Generated artifacts are **not tracked in git** (too large).
Run the adapter to reproduce them:

```bash
python scripts/run_adapter_lmev2.py
```
